PUBLISH_URL="https://username:password@example-publish-app.com"
SECRET_KEY_BASE="secret" # random string for development
ES_HOST="https://www.elastic-host-example.com" # Only if you don't want to use 127.0.0.1:9200 (the default). Don't forget to remove the trailing slash.
