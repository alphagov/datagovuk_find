---
applications:
- name: find-data-beta-staging
  memory: 256M
  buildpack: ruby_buildpack
  env:
    RAILS_ENV: staging
    RACK_ENV: staging
  services:
  - beta-staging-elasticsearch
  - find-staging-secrets
  - logit-ssl-drain
