default: &default
  host: <%= ENV['ES_HOST'] || 'http://localhost:9200' %>
  vcap_services: '<%= ENV["VCAP_SERVICES"] %>'
  elastic_timeout: 5

test:
  <<: *default

development:
  <<: *default

staging:
  <<: *default

production:
  <<: *default
