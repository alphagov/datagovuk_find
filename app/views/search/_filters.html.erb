<%= include_gon(namespace: 'Find') %>

<h2 class="heading-medium"> <%= t('.filter_by') %></h2>

<div class="form-group">
  <label for="location" class="form-label">
    <%= t('.filter_by_geo') %>
  </label>
  <input id="location" name="filters[location]" value="<%= @location %>" class="dgu-filters__filter"/>
</div>

<div class="form-group">
  <label for="<%= t('.accessibility.publisher_label') %>" class="form-label">
    <%= t('.filter_by_pub') %>
  </label>
  <input id="publisher" name="filters[publisher]" value="<%= @organisation %>" class="dgu-filters__filter"/>
</div>

<div class="form-group">
  <%= label_tag :format, t('.filter_by_format'), class: 'form-label' %>
  <%= select_tag('filters[format]', options_for_select(datafile_formats_for_select, selected_format), class: 'form-control dgu-filters__filter') %>
</div>

<div class="form-group">
  <div class="dgu-filters__apply-button">
    <input class="button" type="submit" value="<%= t('.accessibility.submit_button') %>"/>
  </div>
  <div class="dgu-filters__remove-button">
    <%= link_to t('.remove_filters'), search_path(q: @query) %>
  </div>
</div>
